#include <iostream>

int main() {
    double rotations, input;
    std::cin >> input;
    rotations = (input / 4.0);
    std::cout << rotations << std::endl;

    exit(EXIT_SUCCESS);
    std::cin.get();
}